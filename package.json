{
  "name": "psychic-homily-web",
  "version": "1.0.0",
  "scripts": {
    "new-show": "node scripts/parse-show.js",
    "parse-bandcamp": "node scripts/parse-bandcamp.js",
    "new-blog": "node scripts/create-blog-post.js",
    "new-mix": "node scripts/create-mix-post.js",
    "build:prod": "ENVIRONMENT=production cd components && pnpm build:production && cd .. && hugo --config hugo.production.toml --gc --minify",
    "build:stage": "ENVIRONMENT=stage cd components && pnpm build:stage && cd .. && hugo --config hugo.staging.toml --gc --minify",
    "build:components": "cd components && pnpm build",
    "convert-yaml": "node convert-yaml.js",
    "dev": "hugo server",
    "dev:components": "cd components && npm run dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.17.1",
    "dotenv": "^16.4.5",
    "glob": "^10.3.10",
    "js-yaml": "^4.1.0",
    "prompts": "^2.4.2"
  }
}
