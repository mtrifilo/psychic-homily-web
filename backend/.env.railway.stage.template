# Railway Stage Environment Variables Template
# Copy these to Railway dashboard: Project → Stage Environment → Variables
#
# Variables marked [RAILWAY] are auto-provided when you add PostgreSQL/Redis plugins
# Variables marked [COPY] should be copied from your current Coolify .env.stage

# Server Configuration
ENVIRONMENT=stage
API_ADDR=0.0.0.0:8080

# Database Configuration [RAILWAY - auto-provided by PostgreSQL plugin]
# DATABASE_URL will be automatically set when you add PostgreSQL
# You can also set these individually if needed:
# POSTGRES_DB=<from-railway>
# POSTGRES_USER=<from-railway>
# POSTGRES_PASSWORD=<from-railway>
POSTGRES_SSLMODE=disable

# Redis Configuration [RAILWAY - auto-provided by Redis plugin]
# REDIS_URL will be automatically set when you add Redis

# Auth Secrets [COPY from .env.stage]
JWT_SECRET_KEY=<copy-from-coolify>
OAUTH_SECRET_KEY=<copy-from-coolify>
SESSION_SECRET=<copy-from-coolify>
JWT_EXPIRY_HOURS=24

# OAuth Configuration [COPY from .env.stage]
GOOGLE_CLIENT_ID=<copy-from-coolify>
GOOGLE_CLIENT_SECRET=<copy-from-coolify>
GOOGLE_CALLBACK_URL=https://stage.api.psychichomily.com/auth/callback/google

# Session Configuration
SESSION_PATH=/
SESSION_DOMAIN=stage.psychichomily.com
SESSION_MAX_AGE=86400
SESSION_HTTP_ONLY=true
SESSION_SECURE=true
SESSION_SAME_SITE=lax

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://stage.psychichomily.com,https://psychic-homily-web.vercel.app

# Frontend URL
FRONTEND_URL=https://stage.psychichomily.com

# Email Configuration [COPY from .env.stage]
RESEND_API_KEY=<copy-from-coolify>
FROM_EMAIL=noreply@psychichomily.com

# WebAuthn Configuration
WEBAUTHN_RP_ID=stage.psychichomily.com
WEBAUTHN_RP_NAME=Psychic Homily
WEBAUTHN_RP_ORIGINS=https://stage.psychichomily.com

# Logging
LOG_LEVEL=debug
